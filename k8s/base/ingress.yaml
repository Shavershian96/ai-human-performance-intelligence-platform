apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: perf-platform-ingress
  namespace: perf-platform
spec:
  # The kubernetes.io/ingress.class annotation this used to carry has been
  # deprecated since 1.18; kubectl warns on every apply. ingressClassName is
  # the supported field.
  ingressClassName: nginx
  rules:
    - host: perf-platform.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 8000
          - path: /trainer
            pathType: Prefix
            backend:
              service:
                name: ml-trainer
                port:
                  number: 8080
          - path: /ingestion
            pathType: Prefix
            backend:
              service:
                name: data-ingestion
                port:
                  number: 8081
  # In production, add TLS
  # tls:
  #   - hosts:
  #       - perf-platform.local
  #     secretName: perf-platform-tls
